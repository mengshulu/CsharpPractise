@using WebApplication1.Controllers
@model WebApplication1.Controllers.DemoController.Data

@{
    ViewBag.Title = "title";
}

<h2>title</h2>
@using (Html.BeginForm())
{
    @Html.HiddenFor(model => model.Id)
    @Html.AntiForgeryToken()
    <div class="form-horizontal">
        @* <div class="input-group mb-3"> *@
        @*     <input type="text" readonly="" value="@Model.Id" class="form-control" placeholder="Id" aria-label="Id" aria-describedby="basic-addon1"> *@
        @* </div> *@
        <div class="form-group mb-3">
            <div class="input-group col-md-10">
                @* @Html.TextBoxFor(model => model.Title) *@
                @Html.EditorFor(model => model.Title, new { htmlAttributes = new { @class = "form-control" } })
                @* <input type="text" class="form-control" placeholder="Title" aria-label="Title" aria-describedby="basic-addon1"> *@
            </div>
        </div>
        
        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Edit" class="btn btn-default" />
            </div>
        </div>
    </div>
}

